# Where Grafana looks for dashboard definitions dropped into the image.
apiVersion: 1

providers:
  - name: fieldlines
    orgId: 1
    folder: Fieldlines
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
